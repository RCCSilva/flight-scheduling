import { Story, Meta } from '@storybook/react'
import FieldText from './FieldText'

export default {
  title: 'FieldText',
  component: FieldText,
} as Meta

export const Default: Story = () => <FieldText />
